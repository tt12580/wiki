<% provide(:title, "New wiki") %>
<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= simple_form_for [:admin, @wiki] do |f| %>
        <%= f.input :mulu, as: :wysihtml5 %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>
